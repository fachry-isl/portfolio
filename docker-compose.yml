version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: portfolio-frontend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: portfolio-backend
